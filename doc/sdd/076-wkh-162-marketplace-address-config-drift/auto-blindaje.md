# Auto-Blindaje — WKH-162 (marketplace address config-drift)

### [2026-07-08 00:11] Wave C-1 — `import 'server-only'` rompe suite de tests al importar `marketplaceClient` en una ruta

- **Error**: Puse el helper `assertMarketplaceAddressCoherence()` dentro de `src/lib/contracts/marketplaceClient.ts` (que tiene `import 'server-only'` en su top) y lo importé en `creator/earnings/voucher/route.ts`. Eso arrastró el marker `server-only` al grafo de módulos del test existente `voucher-concurrency.test.ts`, que falló al colectar con `Error: This module cannot be imported from a Client Component module` (node_modules/server-only/index.js:1). El typecheck y mi test unitario pasaban, pero la suite completa quedaba en 1 failed suite.
- **Causa raíz**: `server-only` resuelve por la condición `default` → `index.js`, que hace `throw` incondicional bajo vitest (vitest no activa la condición `react-server`). Cualquier test que importe (directa o transitivamente) un módulo con `import 'server-only'` sin mockearlo, revienta al cargar. La ruta del voucher NO importaba server-only antes; mi import lo introdujo.
- **Fix**: Extraje el helper a un módulo PURO nuevo `src/lib/contracts/marketplaceAddressCoherence.ts` (sin `server-only`, sin viem) e importé desde ahí en la ruta del voucher. Revertí la adición a `marketplaceClient.ts`. Así el grafo del voucher route queda limpio, el test de concurrencia existente pasa sin tocarlo, y el helper se testea aislado sin mockear `server-only`. Suite completa: 77 passed / exit 0.
- **Aplicar en**: Antes de importar cualquier módulo con `import 'server-only'` (p.ej. `marketplaceClient.ts`) dentro de una ruta/módulo que esté cubierto por un test de vitest, verificá el grafo. Para lógica pura reutilizable (comparaciones de env, helpers sin side-effects on-chain), preferí un módulo standalone sin `server-only`. Si hay que testear un módulo server-only, mockealo: `vi.mock('server-only', () => ({}))`.
