# SDD-186: Agent Key Scoping

**Issue:** WAS-186  
**Sprint:** 2  
**Status:** APROBADO  
**Migration:** 053  
**Autor:** Spec Reviewer — NexusAgile v1.3  
**Fecha:** 2026-03-13

---

## Wave 0 — Pre-flight (Spec Reviewer)

### Archivos verificados
- [x] `src/features/agent-api/services/agent-keys.service.ts` — existe, revisado
- [x] `src/app/api/v1/compose/route.ts` — existe, revisado
- [x] `supabase/migrations/00000000000003_wasiai_core.sql` — tabla `agent_keys` verificada
- [x] `doc/DB_SCHEMA.md` — columnas de `agent_keys`

### Hallazgos Wave 0

1. ✅ `agent_keys` tiene columna `owner_id` (NO `user_id`). DB_SCHEMA.md tiene un error en esta columna — dice `user_id`. El código y migrations usan `owner_id`.
2. ✅ `agent_keys` actualmente: `id, owner_id, name, key_hash, budget_usdc, spent_usdc, is_active, created_at` — sin `allowed_slugs` ni `allowed_categories`
3. ⚠️ `DB_SCHEMA.md` para `agent_keys` muestra `user_id` en lugar de `owner_id`. Este error debe corregirse en DB_SCHEMA.md.
4. ✅ El compose route usa `createServiceClient()` — bypasa RLS, ownership logic debe ser explícita en código
5. ✅ `agent-keys.service.ts` `createAgentKey()` necesita actualizarse para aceptar `allowed_slugs` y `allowed_categories`

### Dependencias
- WAS-187 depende de WAS-186 (AC-7 de WAS-187 requiere scope check)
- WAS-204 depende de WAS-186 (ownership check)
- Paralelo con WAS-196, WAS-203

---

## Descripción

Añadir scoping a las API keys para restringir qué agentes pueden invocar. Una key puede tener `allowed_slugs` (lista exacta) y/o `allowed_categories`. La lógica es OR: el agente debe estar en allowed_slugs O su categoría en allowed_categories. Sin scope = acceso total (backward compatible).

---

## Wave 1 — DB Migration

**Archivo:** `supabase/migrations/053_agent_key_scoping.sql`

```sql
-- Migration 053: Agent Key Scoping
-- WAS-186 | Sprint 2

ALTER TABLE agent_keys
  ADD COLUMN IF NOT EXISTS allowed_slugs      TEXT[] DEFAULT NULL,
  ADD COLUMN IF NOT EXISTS allowed_categories TEXT[] DEFAULT NULL;

COMMENT ON COLUMN agent_keys.allowed_slugs IS
  'NULL = acceso total. Array de slugs permitidos para esta key.';
COMMENT ON COLUMN agent_keys.allowed_categories IS
  'NULL = sin filtro por categoría. Array de categorías permitidas para esta key.';

-- Índice GIN para búsquedas eficientes en arrays (futuro: CHECK ANY)
CREATE INDEX IF NOT EXISTS idx_agent_keys_allowed_slugs
  ON agent_keys USING GIN (allowed_slugs)
  WHERE allowed_slugs IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_agent_keys_allowed_categories
  ON agent_keys USING GIN (allowed_categories)
  WHERE allowed_categories IS NOT NULL;
```

**DB Security Checklist:**
- ✅ Sin SECURITY DEFINER
- ✅ Sin GRANTs adicionales
- ✅ RLS ya activo en `agent_keys` (policy `agent_keys_owner`)
- ✅ Sin SQL injection
- ✅ Sin RLS bypass

**Build gate Wave 1:** `npx tsc --noEmit`

---

## Wave 2 — Scope Validation en createAgentKey

**Archivo:** `src/features/agent-api/services/agent-keys.service.ts`

### 2.1 Actualizar interfaz AgentKey

```ts
export interface AgentKey {
  // ...existing fields...
  allowed_slugs:      string[] | null
  allowed_categories: string[] | null
}
```

### 2.2 Actualizar createAgentKey

```ts
export async function createAgentKey(
  name: string,
  budgetUsdc: number,
  options?: {
    allowed_slugs?:      string[]
    allowed_categories?: string[]
  }
): Promise<AgentKey> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) throw new Error('Unauthorized')

  // AC-2: Validar slugs al crear key (slugs inexistentes → 422)
  if (options?.allowed_slugs && options.allowed_slugs.length > 0) {
    const { data: foundAgents } = await supabase
      .from('agents')
      .select('slug')
      .in('slug', options.allowed_slugs)
      .eq('status', 'active')
    
    const foundSlugs = new Set((foundAgents ?? []).map(a => a.slug))
    const invalidSlugs = options.allowed_slugs.filter(s => !foundSlugs.has(s))
    
    if (invalidSlugs.length > 0) {
      throw Object.assign(
        new Error(`Slugs no encontrados: ${invalidSlugs.join(', ')}`),
        { code: 'invalid_slugs', status: 422, invalidSlugs }
      )
    }
  }

  const { raw, hash } = generateApiKey()

  const { data, error } = await supabase
    .from('agent_keys')
    .insert({
      owner_id:           user.id,
      name,
      key_hash:           hash,
      budget_usdc:        budgetUsdc,
      allowed_slugs:      options?.allowed_slugs ?? null,
      allowed_categories: options?.allowed_categories ?? null,
    })
    .select()
    .single()

  if (error) throw error
  return { ...(data as AgentKey), raw_key: raw }
}
```

**Build gate Wave 2:** `npx tsc --noEmit`

---

## Wave 3 — Scope Check en Compose y Invoke routes

**Archivos:** `src/app/api/v1/compose/route.ts`

### 3.1 Actualizar KeyRow interface

```ts
interface KeyRow {
  id:                  string
  key_hash:            string
  is_active:           boolean
  budget_usdc:         number
  spent_usdc:          number
  allowed_slugs:       string[] | null  // WAS-186
  allowed_categories:  string[] | null  // WAS-186
}
```

### 3.2 Actualizar select de keyRow

```ts
const { data: keyRow } = await supabase
  .from('agent_keys')
  .select('id, key_hash, is_active, budget_usdc, spent_usdc, allowed_slugs, allowed_categories')
  .eq('key_hash', keyHash)
  .eq('is_active', true)
  .single<KeyRow>()
```

### 3.3 Función de scope check (extraer a lib/scope-check.ts)

**Archivo nuevo:** `src/lib/scope-check.ts`

```ts
/**
 * Verifica si un agente está dentro del scope de una key.
 * Lógica OR: dentro de allowed_slugs OR dentro de allowed_categories.
 * Sin scope (null/null) = acceso total.
 */
export function isAgentInScope(
  agentSlug:     string,
  agentCategory: string,
  allowedSlugs:  string[] | null,
  allowedCategories: string[] | null,
): boolean {
  // AC-5: sin scope = acceso total
  if (!allowedSlugs && !allowedCategories) return true

  // AC-3: lógica OR
  if (allowedSlugs && allowedSlugs.includes(agentSlug)) return true
  if (allowedCategories && allowedCategories.includes(agentCategory)) return true

  return false
}
```

### 3.4 Aplicar scope check en el loop de resolución de agentes (compose)

```ts
// Después de verificar que el agente existe:
for (let i = 0; i < steps.length; i++) {
  const agent = agentMap.get(steps[i].agent_slug)
  if (!agent) {
    return NextResponse.json({ error: 'Agent not found', ... }, { status: 404 })
  }
  // WAS-186: AC-4
  if (!isAgentInScope(agent.slug, agent.category, keyRow.allowed_slugs, keyRow.allowed_categories)) {
    return NextResponse.json(
      { error: 'Agent not in key scope', code: 'scope_violation', slug: agent.slug },
      { status: 403 }
    )
  }
}
```

**Build gate Wave 3:** `npx tsc --noEmit`

---

## Wave 4 — GET /api/v1/agent-keys/me (AC-7)

**Archivo:** `src/app/api/v1/agent-keys/me/route.ts` (crear si no existe) o actualizar endpoint existente

```ts
// El response de getAgentKeys() ya incluirá allowed_slugs/allowed_categories
// porque el select('*') los traerá automáticamente tras la migration.
// Solo verificar que el frontend los pase correctamente.
```

**Build gate Wave 4:** `npx tsc --noEmit`

---

## Wave 5 — Dashboard UI (AC-6)

Mostrar en la tarjeta de cada key:
- Si `allowed_slugs`: lista de slugs permitidos
- Si `allowed_categories`: lista de categorías permitidas
- Si ambos null: badge "Acceso total"

---

## Rollback

1. `ALTER TABLE agent_keys DROP COLUMN IF EXISTS allowed_slugs, DROP COLUMN IF EXISTS allowed_categories;`
2. Revertir `agent-keys.service.ts` y `compose/route.ts`
3. `DROP INDEX IF EXISTS idx_agent_keys_allowed_slugs, idx_agent_keys_allowed_categories;`

---

## Criterios de Aceptación Verificables

| AC | Evidencia |
|----|-----------|
| AC-1 | migration 053 agrega `allowed_slugs TEXT[]` y `allowed_categories TEXT[]` nullable |
| AC-2 | `createAgentKey()` valida slugs contra DB antes de insertar → error 422 con `invalidSlugs` |
| AC-3 | `isAgentInScope()` implementa lógica OR |
| AC-4 | Compose retorna HTTP 403 `{code: "scope_violation"}` |
| AC-5 | `isAgentInScope()` retorna `true` cuando ambos arrays son null |
| AC-6 | Dashboard muestra scope en tarjeta de key |
| AC-7 | `GET /api/v1/agent-keys/me` incluye `allowed_slugs` y `allowed_categories` |

---

## Constraints críticos

- 0.3b: ✅ No toca `checkIpLimit`, `sandbox/balance/route.ts`
- 0.3c: ✅ Sin cambios on-chain
- 0.3d: ✅ Solo ALTER TABLE, sin SECURITY DEFINER, RLS heredado

## Nota para Builder

> ⚠️ `doc/DB_SCHEMA.md` tabla `agent_keys` muestra `user_id` pero la columna real es `owner_id`. Siempre usar `owner_id` en código y migrations. Este SDD ya corrige este error en todos los ejemplos.
