# SDD-219 — Tipado estricto de pagos en agent_calls (payment_type)

**Issue:** WAS-219
**Clasificación:** HU-MAJOR
**Dependencia:** WAS-220 (paths ya corregidos)
**Estado:** SPEC_APPROVED ✓ (2026-03-16)

---

## Context

`agent_calls` no distingue confiablemente entre calls pagadas, free trial y sandbox. WAS-220 ya corrigió los paths de insert para incluir `payment_type` y `agent_slug`. Este issue completa el trabajo: agregar la columna `payment_type` con constraint, backfill rows históricas, y enforcer NOT NULL después de 24h de monitoreo.

### Estado actual post-WAS-220
- invoke/route.ts → ya inserta `payment_type: 'api_key'` y `agent_slug`
- sandbox/invoke/route.ts → ya inserta `payment_type: 'sandbox'` y `agent_slug`
- compose/route.ts → ya inserta `payment_type: 'api_key'` y `agent_slug`
- trial/route.ts, mcp/route.ts, introspect/route.ts → tech debt (no modificados en WAS-220)

### Archivos involucrados
- Migración SQL nueva (columna + constraint + backfill + índice)
- `src/app/api/admin/settlement/route.ts` — filtrar por payment_type (MODIFICAR)
- `src/app/api/v1/agents/[slug]/trial/route.ts` — agregar payment_type (MODIFICAR)
- Migración SQL fase 2 (NOT NULL) — aplicar 24h después

---

## Acceptance Criteria (del backlog-v2)

- AC-1: Audit de todos los paths de insert (ya hecho en WAS-220, verificar completitud)
- AC-2: Cada path incluye `payment_type` explícito: api_key, x402, sandbox, free_trial
- AC-3: Cada path incluye `agent_slug`
- AC-4: Fase 1: columna `payment_type TEXT DEFAULT NULL` + CHECK constraint
- AC-5: Fase 2 (24h después): NOT NULL en `payment_type` y `agent_slug`
- AC-6: `amount_paid` constraint: `CHECK (amount_paid >= 0)` (no > 0)
- AC-7: Backfill rows existentes según heurística
- AC-8: Backfill en dev primero, verificar, luego prod
- AC-9: Settlement filtra `payment_type = 'api_key' AND settled_at IS NULL AND agent_slug IS NOT NULL`
- AC-10: Calls free_trial/sandbox/unknown excluidas de settlement con log DEBUG
- AC-11: Validación a nivel de app antes del insert
- AC-12: Índice `(agent_slug, payment_type, settled_at)` para queries de settlement

---

## Wave 0 — Pre-flight

0.1. Verificar que `payment_type` YA EXISTE como `NOT NULL DEFAULT 'x402'` (migration 032):
```bash
cat supabase/migrations/032_sandbox_credits.sql | grep payment_type
```

0.2. Verificar constraint actual (migration 063 expandió a api_key, free_trial, unknown):
```bash
cat supabase/migrations/063_expand_payment_type_check.sql
```

0.3. Verificar paths de insert ya corregidos por WAS-220:
```bash
grep -rn "payment_type" src/app/api/v1/models/[slug]/invoke/route.ts
grep -rn "payment_type" src/app/api/v1/sandbox/invoke/[slug]/route.ts
grep -rn "payment_type" src/app/api/v1/compose/route.ts
```

0.4. Verificar paths tech debt que FALTAN (inserts sin payment_type explícito):
```bash
grep -n "agent_calls" src/app/api/v1/agents/[slug]/trial/route.ts
grep -n "agent_calls" src/app/api/v1/mcp/route.ts
grep -n "agent_calls" src/app/api/v1/agents/[slug]/introspect/route.ts
```

0.5. Contar rows con payment_type='x402' que podrían estar mal tipadas:
```sql
SELECT payment_type, COUNT(*) FROM agent_calls GROUP BY payment_type;
```

---

## Wave 1 — Migración: backfill + constraint consolidado + índice

**NOTA CLAVE:** La columna `payment_type` ya existe como `TEXT NOT NULL DEFAULT 'x402'` (migration 032). La constraint fue expandida en migration 063 a incluir `api_key`, `free_trial`, `unknown`. Este wave NO crea la columna, solo corrige datos mal tipados y agrega índice.

### W1.1 Crear migración `065_payment_type_backfill.sql`

```sql
-- WAS-219: Backfill payment_type for rows incorrectly defaulted to 'x402'
-- Column already exists as NOT NULL DEFAULT 'x402' (migration 032)
-- Constraint already expanded (migration 063)

-- 1. Backfill: rows that used api_key but got default 'x402'
UPDATE agent_calls SET payment_type = 'api_key'
  WHERE payment_type = 'x402'
  AND key_id IS NOT NULL
  AND tx_hash IS NULL;

-- 2. Backfill: free trial rows that got default 'x402'
UPDATE agent_calls SET payment_type = 'free_trial'
  WHERE payment_type = 'x402'
  AND amount_paid = 0
  AND key_id IS NULL
  AND tx_hash IS NULL;

-- 3. amount_paid >= 0 constraint
ALTER TABLE agent_calls
  DROP CONSTRAINT IF EXISTS chk_amount_paid_positive;
ALTER TABLE agent_calls
  ADD CONSTRAINT chk_amount_paid_non_negative
  CHECK (amount_paid >= 0);

-- 4. Index for settlement queries
CREATE INDEX IF NOT EXISTS idx_agent_calls_settlement
  ON agent_calls (agent_slug, payment_type, settled_at)
  WHERE settled_at IS NULL AND payment_type = 'api_key';
```

### W1.2 Aplicar en dev, verificar distribución
```sql
SELECT payment_type, COUNT(*) FROM agent_calls GROUP BY payment_type;
```

### W1.3 Aplicar en prod

### Build gate W1
```sql
-- Verify no 'x402' rows that should be 'api_key'
SELECT COUNT(*) FROM agent_calls WHERE payment_type = 'x402' AND key_id IS NOT NULL AND tx_hash IS NULL;
-- Expect: 0
```

---

## Wave 2 — Corregir paths tech debt + settlement filter

### W2.1 Agregar campos faltantes a trial/route.ts
El insert actual solo tiene `agent_id, status, latency_ms, is_trial`. Faltan campos requeridos.
Agregar:
```typescript
payment_type: 'free_trial',
agent_slug: slug,       // slug disponible como param del route
amount_paid: 0,         // amount_paid es NOT NULL sin default — trials son gratis
```
**NOTA:** `amount_paid` es `NUMERIC(18,6) NOT NULL` sin default (migration 003). Sin este campo el insert falla silenciosamente o con constraint violation.

### W2.2 Corregir mcp/route.ts (CONFIRMADO: inserta sin payment_type)
Línea ~252 inserta en `agent_calls` sin `payment_type` ni `agent_slug`. Recibe default 'x402' incorrecto (MCP usa key auth).
Agregar:
```typescript
payment_type: 'api_key',
agent_slug: model.slug,
```

### W2.2b Corregir introspect/route.ts (CONFIRMADO: inserta en línea 137)
Inserta en `agent_calls` sin `payment_type` ni `agent_slug`. Recibe default 'x402' incorrecto.
Agregar:
```typescript
payment_type: 'api_key',
agent_slug: slug,  // slug ya disponible en el contexto del route param
```

### W2.3 Actualizar settlement filter
En `src/app/api/admin/settlement/route.ts`, cambiar query de pending calls:
```typescript
// Antes:
.is('settled_at', null)
// Después:
.is('settled_at', null)
.eq('payment_type', 'api_key')
.not('agent_slug', 'is', null)
```

Agregar log para calls excluidas:
```typescript
console.debug(`[settlement] Excluding ${excluded} calls (non api_key or no slug)`)
```

### W2.4 Validación pre-insert (app level)
Crear helper `src/lib/validation/payment-type.ts`:
```typescript
export const VALID_PAYMENT_TYPES = ['api_key', 'x402', 'free_trial', 'sandbox', 'unknown'] as const
export type PaymentType = typeof VALID_PAYMENT_TYPES[number]

export function assertPaymentType(value: string): asserts value is PaymentType {
  if (!VALID_PAYMENT_TYPES.includes(value as PaymentType)) {
    throw new Error(`Invalid payment_type: ${value}`)
  }
}
```

### Build gate W2
```bash
npx next build 2>&1 | grep -c "error"
# Test invoke → verify payment_type in DB
# Test settlement → verify filter works
```

---

## Wave 3 — Verificación + agent_slug NOT NULL (deferred)

**NOTA:** `payment_type` ya es `NOT NULL DEFAULT 'x402'` desde migration 032. No necesita fase 2 para NOT NULL.

### W3.1 Verificar 0 rows mal tipadas después de 24h de monitoreo
```sql
-- Verificar que no hay nuevos 'x402' que deberían ser 'api_key'
SELECT COUNT(*) FROM agent_calls
WHERE payment_type = 'x402'
AND key_id IS NOT NULL
AND tx_hash IS NULL
AND called_at > NOW() - INTERVAL '24 hours';
-- MUST be 0
```

### W3.2 agent_slug NOT NULL (DEFERRED)
36 legacy rows tienen `agent_slug = NULL`. Estas rows ya están marcadas como `payment_type = 'unknown'` o `'x402'` default. NOT NULL en agent_slug requiere:
1. Backfill o delete de las 36 rows legacy
2. Solo aplicar después de review manual

```sql
-- DEFERRED — no aplicar automáticamente
-- ALTER TABLE agent_calls ALTER COLUMN agent_slug SET NOT NULL;
```

### Build gate W3
```sql
SELECT payment_type, COUNT(*) FROM agent_calls
WHERE called_at > NOW() - INTERVAL '24 hours'
GROUP BY payment_type;
-- Expect: solo api_key, x402, sandbox, free_trial (no unknown)
```

---

## Rollback

### Fase 1 (solo revierte lo que ESTE SDD agregó — NO drop column, que es de migration 032)
```sql
DROP INDEX IF EXISTS idx_agent_calls_settlement;
ALTER TABLE agent_calls DROP CONSTRAINT IF EXISTS chk_amount_paid_non_negative;
-- Revertir backfill: no es práctico, las rows quedan con los valores corregidos
-- La columna payment_type NO se dropea — existía antes de este SDD (migration 032)
```

### agent_slug NOT NULL (si se aplicó)
```sql
ALTER TABLE agent_calls ALTER COLUMN agent_slug DROP NOT NULL;
```

---

## Critical Constraints

- **Deploy order:** Fase 1 → deploy app → monitorear 24h → Fase 2
- **NO aplicar NOT NULL en `agent_slug`** todavía — 36 rows legacy con NULL necesitan review
- **amount_paid >= 0** no `> 0` — calls fallidas con 0 son válidas
- Aplicar migración en dev Y prod simultáneamente
- PROHIBIDO modificar trial/route.ts más allá de agregar payment_type/agent_slug al insert
- Settlement SOLO liquida `payment_type = 'api_key'` — todo lo demás excluido
- ESLint `--max-warnings 0`
