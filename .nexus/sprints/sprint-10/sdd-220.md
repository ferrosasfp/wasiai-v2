# SDD #WAS-220: Audit y corrección de paths de insert en agent_calls

> SPEC_APPROVED: yes
> Fecha: 2026-03-16
> Tipo: tech-task
> SDD_MODE: mini
> Clasificación: TASK (prereq de WAS-219)
> Branch: task/220-agent-calls-insert-audit
> Artefactos: .nexus/sprints/sprint-10/

---

## 1. Resumen

Audit exhaustivo de todos los paths que insertan en `agent_calls`. Todos deben setear `payment_type` y `agent_slug` explícitamente. Sin este fix, el constraint NOT NULL de WAS-219 rompe producción.

**Hallazgos del audit (insert-audit.md):**
- `x402Handler.ts` NO existe — la lógica x402 está inlineada en `invoke/route.ts`
- El CHECK constraint actual solo permite `('x402', 'sandbox')` — `'api_key'` y `'free_trial'` fallarían hoy
- `logCall()` en invoke/route.ts no recibe `payment_type` — firma a extender
- Hay 3 paths adicionales fuera de scope original: `trial/route.ts`, `mcp/route.ts`, `introspect/route.ts`

---

## 2. Work Item

| Campo | Valor |
|-------|-------|
| **#** | WAS-220 |
| **Tipo** | tech-task |
| **Objetivo** | Todos los inserts en agent_calls incluyen payment_type y agent_slug válidos; CHECK constraint expandido |
| **Scope IN** | Los 4 paths principales + migración SQL del CHECK constraint |
| **Scope OUT** | Schema NOT NULL constraint (es WAS-219), UI, contrato, paths trial/mcp/introspect (documentar como out-of-scope en insert-audit.md) |

### Acceptance Criteria (EARS)

1. WHEN se ejecuta el audit, THE documento `insert-audit.md` SHALL listar TODOS los paths con su estado actual (incluyendo trial, mcp, introspect)
2. WHEN el CHECK constraint se expande, THE `agent_calls.payment_type` SHALL aceptar `('x402', 'sandbox', 'api_key', 'free_trial', 'unknown')`
3. WHEN invoke con api_key completa, THE insert en `agent_calls` SHALL incluir `payment_type = 'api_key'` y `agent_slug` no null
4. WHEN invoke con x402 completa, THE insert SHALL incluir `payment_type = 'x402'` y `agent_slug` no null
5. WHEN sandbox invoke completa, THE insert SHALL incluir `payment_type = 'sandbox'` y `agent_slug` no null
6. WHEN compose invoke completa, THE insert SHALL incluir `payment_type = 'api_key'` y `agent_slug = agent.slug` no null
7. AFTER correcciones, THE ambiente de dev SHALL generar 0 inserts en `agent_calls` sin `agent_slug` o sin `payment_type` via invoke, sandbox, y compose paths en 30 minutos de uso normal

---

## 3. Context Map

### Estado real del código (verificado por audit)

| Archivo | Estado actual | Cambio necesario |
|---------|--------------|-----------------|
| `src/app/api/v1/models/[slug]/invoke/route.ts` | `logCall()` existe, sin `payment_type`. Route A=api_key, Route B=x402. Slug disponible. | Agregar `paymentType: string` a la firma de `logCall()` |
| `src/app/api/v1/sandbox/invoke/[slug]/route.ts` | Dos inserts sin `payment_type` ni `agent_slug`. Slug disponible vía params. | Agregar ambos campos a ambos inserts |
| `src/app/api/v1/compose/route.ts` | `executeStep()` con dos inserts sin `payment_type` ni `agent_slug`. `agent.slug` disponible. | Agregar `payment_type: 'api_key'`, `agent_slug: agent.slug` |
| Migración SQL nueva | CHECK constraint actual: `('x402', 'sandbox')` | Expandir a `('x402', 'sandbox', 'api_key', 'free_trial', 'unknown')` |
| `src/app/api/v1/trial/route.ts` | Insert sin payment_type — OUT OF SCOPE | Documentar en insert-audit.md como deuda técnica |
| `src/app/api/v1/mcp/route.ts` | Insert sin payment_type — OUT OF SCOPE | Documentar en insert-audit.md como deuda técnica |
| `src/app/api/v1/introspect/route.ts` | Insert sin payment_type — OUT OF SCOPE | Documentar en insert-audit.md como deuda técnica |

---

## 4. Archivos afectados

| Archivo | Acción | Qué cambia | Exemplar |
|---------|--------|-----------|----------|
| `src/app/api/v1/models/[slug]/invoke/route.ts` | Modificar | Extender firma de `logCall()` con `paymentType: string`; pasar `'api_key'` desde Route A, `'x402'` desde Route B | Mismo archivo |
| `src/app/api/v1/sandbox/invoke/[slug]/route.ts` | Modificar | Agregar `payment_type: 'sandbox'` y `agent_slug: slug` a ambos inserts (schema_violation y normal) | invoke/route.ts |
| `src/app/api/v1/compose/route.ts` | Modificar | En `executeStep()`: agregar `payment_type: 'api_key'` y `agent_slug: agent.slug` a ambos inserts | invoke/route.ts |
| `supabase/migrations/[timestamp]_expand_payment_type_check.sql` | Crear | `ALTER TABLE agent_calls DROP CONSTRAINT agent_calls_payment_type_check; ALTER TABLE agent_calls ADD CONSTRAINT agent_calls_payment_type_check CHECK (payment_type IN ('x402', 'sandbox', 'api_key', 'free_trial', 'unknown'));` | Migraciones existentes en supabase/migrations/ |
| `.nexus/sprints/sprint-10/insert-audit.md` | Crear | Reporte del audit incluyendo paths out-of-scope | N/A |

---

## 5. Acceptance Criteria verificables

1. `grep -r "agent_calls" src/ --include="*.ts" | grep "insert\|upsert"` — todos los resultados en invoke, sandbox, compose incluyen `payment_type`
2. Constraint expandida: `SELECT conname, consrc FROM pg_constraint WHERE conname = 'agent_calls_payment_type_check'` → incluye `'api_key'`
3. Query en dev post-fix: `SELECT COUNT(*) FROM agent_calls WHERE (payment_type IS NULL OR agent_slug IS NULL) AND created_at > NOW() - INTERVAL '30 minutes'` → 0 (solo para paths en scope: invoke, sandbox, compose)

---

## 6. Constraint Directives

### OBLIGATORIO
- Documentar hallazgos en `insert-audit.md` ANTES de hacer cambios
- Ejecutar la migración SQL en dev antes de probar los handlers corregidos
- Probar cada path corregido en dev (curl o test)

### PROHIBIDO
- NO agregar el constraint NOT NULL al schema (eso es WAS-219)
- NO modificar `trial/route.ts`, `mcp/route.ts`, `introspect/route.ts` (out of scope)
- NO refactorizar la lógica de negocio de los handlers — solo agregar los campos faltantes
- NO cambiar el nombre de la función `logCall()` ni su contrato de retorno

---

## 7. Wave Plan

### Wave 0 — Audit y migración SQL
- [ ] W0.1: Leer los 7 archivos identificados — documentar en `insert-audit.md`
- [ ] W0.2: Ejecutar migración SQL para expandir CHECK constraint en dev y prod

### Wave 1 — Correcciones en handlers
- [ ] W1.1: `invoke/route.ts` — extender `logCall(paymentType)`: Route A → `'api_key'`, Route B → `'x402'`
- [ ] W1.2: `sandbox/invoke/[slug]/route.ts` — agregar `payment_type: 'sandbox'` y `agent_slug: slug` a ambos inserts
- [ ] W1.3: `compose/route.ts` — agregar `payment_type: 'api_key'` y `agent_slug: agent.slug` en `executeStep()`
- [ ] BUILD GATE: `npm run build` — 0 errores, 0 warnings

### Wave 2 — Verificación
- [ ] W2.1: Invocar cada path en dev y verificar inserts tienen los campos (curl o test manual)
- [ ] W2.2: Query de verificación: 0 nulls en dev en 30 min (solo paths in-scope)

### Rollback
Si algún handler se rompe: `git revert <commit>` del handler específico. La DB solo tiene CHECK expandido (no NOT NULL), sin riesgo de datos corruptos.

---

*SDD generado por NexusAgil — MINI — WAS-220 (post spec-review)*
